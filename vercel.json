{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "outputDirectory": "public",
        "includeFiles": ["./data/**"]
      }
    }
  ],
  "routes": [{ "src": "/graphql", "dest": "index.js" }]
}
